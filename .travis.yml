language: cpp
dist: bionic
cache: False
sudo: required
addons:
  apt:
    packages: 
     - libboost-all-dev
     - libgtest-dev
     - cmake
compiler:
  - clang
  - gcc
script: 
  - mkdir -p build
  - cd build
  - cmake ..
  - make
